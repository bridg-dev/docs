# $messageId
Отправляет ID сообщение
### Использование
```php
$messageId
```

## Пример(ы)

```javascript
bot.command({
  name: 'messageId',
  code: `
$messageId`
// Возвращает: ID сообщение где была указана команда
})
```
