# $reboot
Возвращает перезапуск файла или бота
### Использование
```php
$reboot
```

## Пример(ы)

```javascript
module.exports = [{
  name: 'reboot',
  code: `
$reboot[file.js]`
// Возвращает: перезапуск файла
}]
```
